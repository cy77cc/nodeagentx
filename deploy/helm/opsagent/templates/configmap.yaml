apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "opsagent.configName" . }}
  labels:
    {{- include "opsagent.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
